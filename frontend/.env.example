# Frontend (Vite) — PUBLIC values only. Everything here is compiled into the browser
# bundle, so NEVER put the Shopify API secret or the DB password here.
VITE_API_BASE_URL=http://127.0.0.1:5005
# Shopify API key is public/embeddable (the secret stays in backend/.env):
VITE_SHOPIFY_API_KEY=
